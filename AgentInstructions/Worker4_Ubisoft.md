# Worker 4: Ubisoft Connect Executable Generator

## Objective
Implement a solution that takes all uninstalled games from Ubisoft Connect and generates a dummy `.exe` file for each one. When executed, this `.exe` must automatically trigger the Ubisoft install prompt.

## Context & Requirements
- **Target:** Ubisoft Connect (`Platform = 'Ubisoft'`).
- **Command Logic:** Ubisoft uses a simple, reliable URI scheme.
- **Execution Command to Embed:** 
  ```csharp
  Process.Start(new ProcessStartInfo {
      FileName = $"uplay://install/{GameID}",
      UseShellExecute = true
  });
  ```
- **Task:** Write a utility to dynamically compile dummy executables for uninstalled Ubisoft games containing this logic. 
- **Steam Syncing:** After generating the dummy `.exe`, sync the game to Steam using this `.exe` as the target shortcut. You must pull the customized (grayscaled) images from the internal image cache (e.g., `SteamSync\Cache\UninstalledImages\{Platform}\{GameID}_cover.jpg`) generated by **Worker 1**, and apply them to the new Steam shortcut's official AppID grid files. This ensures it appears as a clear "Download Game" link in the Steam library.

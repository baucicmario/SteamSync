using System; using System.IO; using Microsoft.Win32; class Program { static void Main() { string bnetPath = null; using (RegistryKey key = Registry.LocalMachine.OpenSubKey(@"SOFTWARE\WOW6432Node\Microsoft\Windows\CurrentVersion\Uninstall\Battle.net")) { if (key != null) bnetPath = key.GetValue("InstallLocation") as string; } Console.WriteLine("bnetPath: " + bnetPath); string exePath = Path.Combine(bnetPath ?? "", "Battle.net.exe"); Console.WriteLine("exePath: " + exePath); Console.WriteLine("Exists: " + File.Exists(exePath)); } }
